'use strict';

// web3keys background worker entrypoint (Phase 1+). Will host BullMQ queues for
// chain sync, incoming-payment notifications, email, and webhook delivery.

console.log('web3keys worker: no jobs configured yet (Phase 1)');
